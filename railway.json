{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn rbproduct.wsgi --bind 0.0.0.0:$PORT --workers=2 --timeout 120"
  }
}